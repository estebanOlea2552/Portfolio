import { EducacionDirective } from './educacion.directive';

describe('EducacionDirective', () => {
  it('should create an instance', () => {
    const directive = new EducacionDirective();
    expect(directive).toBeTruthy();
  });
});
